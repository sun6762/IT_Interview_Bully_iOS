# 面向协议编程

- 来源：`Swift/面向协议编程.md`
- 题目数：5

## Q1. 什么是协议？

Swift 相关题建议从“语言机制如何影响架构”回答：值语义提升并发安全，协议与泛型提升抽象复用，派发机制影响性能与可替换性。Combine/Rx 重点在背压、线程调度和生命周期管理；业务层要保证订阅释放、错误收敛和可测试性（可注入 scheduler / mock publisher）。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。

### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「什么是协议？」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「什么是协议？」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？

## Q2. Swift 和 OC 的协议有什么不同？

Swift 相关题建议从“语言机制如何影响架构”回答：值语义提升并发安全，协议与泛型提升抽象复用，派发机制影响性能与可替换性。Combine/Rx 重点在背压、线程调度和生命周期管理；业务层要保证订阅释放、错误收敛和可测试性（可注入 scheduler / mock publisher）。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。

### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「Swift 和 OC 的协议有什么不同？」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「Swift 和 OC 的协议有什么不同？」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？

## Q3. 面相协议编程的特点

Swift 相关题建议从“语言机制如何影响架构”回答：值语义提升并发安全，协议与泛型提升抽象复用，派发机制影响性能与可替换性。Combine/Rx 重点在背压、线程调度和生命周期管理；业务层要保证订阅释放、错误收敛和可测试性（可注入 scheduler / mock publisher）。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。

### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「面相协议编程的特点」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「面相协议编程的特点」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？

## Q4. 面向协议编程和面相对象编程的区别

Swift 相关题建议从“语言机制如何影响架构”回答：值语义提升并发安全，协议与泛型提升抽象复用，派发机制影响性能与可替换性。Combine/Rx 重点在背压、线程调度和生命周期管理；业务层要保证订阅释放、错误收敛和可测试性（可注入 scheduler / mock publisher）。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。

### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「面向协议编程和面相对象编程的区别」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「面向协议编程和面相对象编程的区别」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？

## Q5. 什么时候使用面向协议编程？

Swift 相关题建议从“语言机制如何影响架构”回答：值语义提升并发安全，协议与泛型提升抽象复用，派发机制影响性能与可替换性。Combine/Rx 重点在背压、线程调度和生命周期管理；业务层要保证订阅释放、错误收敛和可测试性（可注入 scheduler / mock publisher）。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。
### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「什么时候使用面向协议编程？」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「什么时候使用面向协议编程？」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？
