# 泛型

- 来源：`Swift/泛型.md`
- 题目数：7

## Q1. 什么是泛型？

Swift 相关题建议从“语言机制如何影响架构”回答：值语义提升并发安全，协议与泛型提升抽象复用，派发机制影响性能与可替换性。Combine/Rx 重点在背压、线程调度和生命周期管理；业务层要保证订阅释放、错误收敛和可测试性（可注入 scheduler / mock publisher）。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。

### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「什么是泛型？」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「什么是泛型？」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？

## Q2. 泛型在集合中的使用

Swift 相关题建议从“语言机制如何影响架构”回答：值语义提升并发安全，协议与泛型提升抽象复用，派发机制影响性能与可替换性。Combine/Rx 重点在背压、线程调度和生命周期管理；业务层要保证订阅释放、错误收敛和可测试性（可注入 scheduler / mock publisher）。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。

### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「泛型在集合中的使用」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「泛型在集合中的使用」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？

## Q3. 使用泛型结合 @propertyWrapple

Swift 相关题建议从“语言机制如何影响架构”回答：值语义提升并发安全，协议与泛型提升抽象复用，派发机制影响性能与可替换性。Combine/Rx 重点在背压、线程调度和生命周期管理；业务层要保证订阅释放、错误收敛和可测试性（可注入 scheduler / mock publisher）。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。

### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「使用泛型结合 @propertyWrapple」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「使用泛型结合 @propertyWrapple」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？

## Q4. 泛型的约束

Swift 相关题建议从“语言机制如何影响架构”回答：值语义提升并发安全，协议与泛型提升抽象复用，派发机制影响性能与可替换性。Combine/Rx 重点在背压、线程调度和生命周期管理；业务层要保证订阅释放、错误收敛和可测试性（可注入 scheduler / mock publisher）。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。

### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「泛型的约束」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「泛型的约束」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？

## Q5. 使用协议约束泛型

Swift 相关题建议从“语言机制如何影响架构”回答：值语义提升并发安全，协议与泛型提升抽象复用，派发机制影响性能与可替换性。Combine/Rx 重点在背压、线程调度和生命周期管理；业务层要保证订阅释放、错误收敛和可测试性（可注入 scheduler / mock publisher）。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。

### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「使用协议约束泛型」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「使用协议约束泛型」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？

## Q6. 使用 where 子句约束泛型

Swift 相关题建议从“语言机制如何影响架构”回答：值语义提升并发安全，协议与泛型提升抽象复用，派发机制影响性能与可替换性。Combine/Rx 重点在背压、线程调度和生命周期管理；业务层要保证订阅释放、错误收敛和可测试性（可注入 scheduler / mock publisher）。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。

### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「使用 where 子句约束泛型」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「使用 where 子句约束泛型」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？

## Q7. 使用集成约束泛型

Swift 相关题建议从“语言机制如何影响架构”回答：值语义提升并发安全，协议与泛型提升抽象复用，派发机制影响性能与可替换性。Combine/Rx 重点在背压、线程调度和生命周期管理；业务层要保证订阅释放、错误收敛和可测试性（可注入 scheduler / mock publisher）。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。
### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「使用集成约束泛型」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「使用集成约束泛型」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？
