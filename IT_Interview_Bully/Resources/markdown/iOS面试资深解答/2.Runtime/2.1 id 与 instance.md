# 2.1 id 与 instance

- 来源：`2.Runtime/2.1 id 与 instance.md`
- 题目数：1

## Q1. 2.1 id 与 instance

Runtime 题核心是“动态性成本与边界”。消息发送由 `objc_msgSend` 基于 `isa` 查找缓存与方法列表；找不到进入动态解析与消息转发链。资深实践中 swizzling 仅用于可控、可回滚场景（埋点、防护、兼容），必须保证幂等、线程安全、和调用原实现；另外要评估调试可观测性与后续维护成本。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。
### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「2.1 id 与 instance」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「2.1 id 与 instance」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？
