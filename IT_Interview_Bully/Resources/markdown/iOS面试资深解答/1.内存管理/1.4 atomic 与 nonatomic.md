# 1.4 atomic 与 nonatomic

- 来源：`1.内存管理/1.4 atomic 与 nonatomic.md`
- 题目数：1

## Q1. 1.4 atomic 与 nonatomic

`atomic` 只保证 setter/getter 的原子性，不保证复合操作线程安全，也不等价于锁。实际项目默认 `nonatomic` 以减少开销，线程安全通过更高层策略实现：串行队列、不可变数据、或细粒度锁。若是跨线程共享可变状态，优先用 actor/queue 隔离而不是依赖属性修饰符。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。
### 代码示例

```swift
final class SSImageDecoder {
    private let queue = DispatchQueue(label: "com.ss.decode", qos: .userInitiated)

    func decode(_ data: Data, completion: @escaping (UIImage?) -> Void) {
        queue.async {
            let image = UIImage(data: data)
            DispatchQueue.main.async { completion(image) }
        }
    }
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「1.4 atomic 与 nonatomic」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「1.4 atomic 与 nonatomic」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？
