# 3.3 栈 block

- 来源：`3.Block/3.3 栈 block.md`
- 题目数：1

## Q1. 3.3 栈 block

Block/闭包题加分点在内存布局与捕获语义：全局块在数据区，栈块需拷贝到堆后长期持有；捕获值类型是拷贝，捕获引用类型是引用计数管理。`__block` 改变的是变量存储与可变性，不自动解决循环引用。资深代码会统一 `[weak self]` + guard 语义，明确强持有窗口，并在异步链路中设置取消策略。

**面试加分回答框架：**
1. 先给结论与适用边界。
2. 说明底层机制或关键原理。
3. 给出真实项目中的坑与治理方案。
4. 补充如何验证（指标 / 工具 / 回归方式）。
### 代码示例

```swift
protocol SSQuestionRepository {
    func fetchQuestions() -> AnyPublisher<[String], Error>
}
```

### 追问点

- 如果这个问题在高并发或弱网场景下会放大什么风险？（围绕「3.3 栈 block」）
- 线上出现同类问题时，你会先看哪三个指标或日志？
- 这个方案在团队协作中如何避免被后续迭代破坏？

### 反问点

- 你们线上与「3.3 栈 block」相关最常见事故是什么？通常怎么复盘？
- 团队对性能/稳定性是否有明确基线和回归门禁？
- 该模块是否有可观测体系（崩溃、卡顿、内存、耗电）和告警？
